<?xml version="1.0" encoding="utf-8"?>
<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">

    <CheckBoxPreference
        android:key="pref_init"
        android:title="Initialized"
        android:summary="App has been initialized on first run"
        android:defaultValue="true"/>
    <EditTextPreference
        android:key="pref_save"
        android:title="Keep how many days"
        android:summary="Keep how many days on Google Drive 0-30"
        android:defaultValue="7"
        android:inputType="number"
        />

</PreferenceScreen>